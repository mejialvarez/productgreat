<h1>Productos</h1>

<table>
  <thead>
    <tr>
      <td>Id</td>
      <td>Nombre</td>
      <td>URL</td>
      <td>Descripción</td>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.id %></td>
        <td><%= product.name %></td>
        <td><%= product.url %></td>
        <td><%= product.description %></td>
      </tr>
    <% end %>
  </tbody>
</table>